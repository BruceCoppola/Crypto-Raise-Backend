export default function Home() {
  return (
    <main style={{ padding: '2rem', fontFamily: 'system-ui, -apple-system, Segoe UI, Roboto, Arial' }}>
      <h1>Startup Raise — Backend</h1>
      <p>This Next.js app exposes stubbed API routes under <code>/api/*</code>.</p>
      <p>Try: <code>/api/kyc/start</code>, <code>/api/accredit/start</code>, <code>/api/payments/checkout</code>, <code>/api/captable/issue</code>, <code>/api/documents/compliance-pack</code></p>
    </main>
  );
}
